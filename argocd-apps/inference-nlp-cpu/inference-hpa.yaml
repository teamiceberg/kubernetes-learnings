apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: sentianal-cpu-infer-hpa
  namespace: cpu-inference
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: sentianal-cpu-infer
  minReplicas: 2
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
