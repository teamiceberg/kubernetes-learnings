apiVersion: v1
kind: Service
metadata:
  name: nlp-cpu-infer-svc
  namespace: cpu-inference
spec:
  selector:
    app: nlp-cpu-infer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
