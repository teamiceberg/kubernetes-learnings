apiVersion: v1
kind: Service
metadata:
  name: nlp-cpu-infer-svc
  namespace: cpu-inference
spec:
  selector:
    app: sentianal-cpu-infer-app
  ports:
    - protocol: TCP
      port: 5080
      targetPort: 5080
  type: ClusterIP
