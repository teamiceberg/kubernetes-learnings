apiVersion: v1
kind: Service
metadata:
  name: nlp-cpu-infer-svc
  namespace: cpu-inference
spec:
  selector:
    app: sentianal-cpu-infer-app
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5080
  
